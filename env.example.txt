APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:qXH1RG3utslAH8/8t82SVXQlmcyK/fgU678J28SxQi4=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your-email@gmail.com
MAIL_FROM_NAME="SwiftPay"

# Google OAuth
# Get these from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=/auth/google/callback

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=your-database-password
# CACHE_DRIVER=file

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

# Redis Configuration (Optional - disabled by default)
# Redis is a drop-in accelerator, not a dependency
REDIS_ENABLED=false
REDIS_CLIENT=phpredis

# Redis Provider: 'upstash' (managed) or 'self-hosted' (default)
# When using Upstash, provide REDIS_URL only
# When using self-hosted, provide REDIS_HOST, REDIS_PORT, etc.
REDIS_PROVIDER=self-hosted

# For Upstash (when REDIS_PROVIDER=upstash)
# Get this from your Upstash dashboard: redis://default:password@host:port
# REDIS_URL=redis://default:your-password@your-host.upstash.io:6379

# For Self-hosted Redis (when REDIS_PROVIDER=self-hosted)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=null
REDIS_USERNAME=null
REDIS_DB=0
REDIS_CACHE_DB=1

# Redis Feature Flags (all disabled by default)
# Enable these only after REDIS_ENABLED=true and Redis is properly configured
REDIS_LOCKS_ENABLED=false
REDIS_IDEMPOTENCY_ENABLED=false
REDIS_QUEUES_ENABLED=false

# Lock Configuration
LOCK_DRIVER=database
LOCK_TIMEOUT=10
LOCK_EXPIRATION=300

# Idempotency Configuration
IDEMPOTENCY_DRIVER=database
IDEMPOTENCY_TTL=86400


AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Umami Analytics (optional - privacy-first analytics)
# UMAMI_WEBSITE_ID=your-website-id
# UMAMI_SCRIPT_URL=https://cloud.umami.is/script.js
# UMAMI_DO_NOT_TRACK=true
# UMAMI_HOST_URL=  # Self-hosted instance base URL (e.g. https://analytics.example.com); set data-host-url on script
